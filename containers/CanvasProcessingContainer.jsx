import { connect } from 'react-redux';
import CanvasProcessing from '../components/CanvasProcessing.jsx';

const mapStateToProps = (state) => ({
});

const mapDispatchToProps = (dispatch, ownProps) => ({
});

const CanvasProcessingContainer = connect(
	mapStateToProps,
	mapDispatchToProps
)(CanvasProcessing);

export default CanvasProcessingContainer;
